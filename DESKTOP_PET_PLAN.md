# 桌宠制作计划

## 1. 当前基线与实施状态

- 比例校正稿：`assets/reference/character-actions-adjusted-v1.png`
- 比例母版：`06_idle`
- 站姿目标：约 `2.45` 头身
- 原始拼图是 RGB/JPG，棋盘格已烘焙，不能直接用于桌宠；现已重建透明生产表，并拆分为 18 张 `512 × 512` RGBA 精灵。
- 现有 18 张图是静态关键姿势，不是连续动画帧。

截至 2026-08-20：M0–M4 已完成；v0.3.0 保留逐帧播放器、24 帧 LRU 缓存及旧素材故障回退，并取消正常调度中的所有进入、退出、启动和落地转场。

## 2. 素材生产闸门（M0）

已交付 18 张独立 `512 × 512` RGBA PNG。生产清单位于 `assets/manifest/assets.json`，比例总览位于 `assets/review/contact-sheet.png`。

### 统一规则

- 以 `06_idle` 锁定脸型、发型、眼镜、眼距、服装纹样、手掌和鞋子比例。
- 先把检测到的面部宽度归一到 `98 px`，再以整体观感校正站姿；因为素材是扁平 PNG，最终允许适度改变头身比。
- 12 个站立动作的透明可见高度统一为 `340 px`，脚底基线统一为画布 `y = 464 px`。
- 坐姿、睡姿和趴姿保持同一头部尺度，自然变矮，禁止为了填满画布而整体放大。
- 大幅比例校正在离线素材阶段完成；运行期只保留呼吸等小幅动态缩放。
- 四周保留至少 `48 px` 透明安全区，手、头发、鞋和书页不得裁切。
- 在黑、白、洋红三种底色检查发丝和衣袖边缘，不得残留棋盘格、白边或蓝边。

### 锚点

- 站立/阅读/爱心/挥手：双脚接地点中点为 `groundAnchor`。
- `03/04`：增加 `seatAnchor`，用于吸附窗口或任务栏边缘。
- `10/13/18`：以实际最低接触点为 `floorAnchor`。
- `01`：使用屏幕边缘 `edgeAnchor`，并生成左右镜像版本。
- `02`：使用骨盆/重心 `pivot`，不套用站姿地面基线。

每张素材写入 `assets.json`：

```json
{
  "id": "idle.neutral",
  "sourceSize": [1280, 1280],
  "visualBounds": [0, 0, 0, 0],
  "pivot": [0.5, 0.5],
  "groundAnchor": [0.5, 0.9],
  "hitMask": "masks/idle.neutral.png",
  "durationMs": 1200,
  "interruptible": true,
  "weight": 1,
  "cooldownMs": 0,
  "nextStates": []
}
```

## 3. Windows 桌宠壳（M1）

首版采用 `.NET 8 + WPF`，项目建议拆为：

- `DesktopPet.App`：透明窗体、托盘、设置、启动项。
- `DesktopPet.Core`：时间轴、状态机、动作调度、配置。
- `Assets`：PNG、命中蒙版、manifest。
- `DesktopPet.Tests`：单元、视觉和窗口交互测试。

第一阶段只完成并验证：无边框透明窗、置顶、任务栏隐藏、拖拽、位置恢复、单实例，以及透明区域能把点击传给下层程序。

命中模式提供三档：

1. 仅人物像素可点（默认）
2. 整个窗口可点
3. 完全点击穿透

完全穿透必须能从托盘菜单或全局热键恢复，避免桌宠失控。

## 4. 动作与状态机（M2）

| 状态 | 动作编排 |
| --- | --- |
| 启动 | `01_peek → 06_idle` |
| 待机 | `06 ↔ 07`，`05` 负责眨眼/闭眼笑 |
| 坐姿 | `03 ↔ 04` |
| 阅读 | `08 → 09`，`12` 为偶发惊喜 |
| 睡眠 | `10 ↔ 13`，鼠标交互唤醒 |
| 爱心 | `11 ↔ 14 → 06` |
| 招手 | `17 → 15 → 16 → 15 → 06` |
| 写字 | `18` 长循环 |
| 拖拽/释放 | `02` 跟随、短弧线或回弹，落地回 `06` |

顶层状态：`Boot / Peek / Idle / Sitting / Reading / Sleeping / Heart / Waving / Writing / Dragging / Falling / Menu / Hidden`。

优先级：`Menu > Dragging > 直接点击反馈 > 睡眠或计划活动 > 随机待机`。

MVP 只对整张图做轻微平移、旋转、呼吸缩放和遮挡点换姿，不做形变插帧。首版不承诺自主行走；若要自然行走和落地，需要新增：

- `walk`：6–8 帧
- `landing`：3–4 帧
- `sit_enter/sit_exit`：各 3–4 帧

## 5. 设置与桌面适配（M3）

- 大小：`50%–200%`
- 透明度、始终置顶、锁定/穿透
- 动作频率、睡眠等待、减少动态效果
- 音效和开机启动
- 多显示器、负坐标副屏、100%/125%/150%/200% DPI
- 屏幕边缘吸附、拔屏后的自动回收
- 配置使用带 `schemaVersion` 的 JSON，原子写入并保留 `.bak`

若需要运行时单独调节头身比，素材必须进一步拆为 `head/body/prop` 三层并记录颈部锚点；扁平 PNG 只能整体缩放。

## 6. 验收与发布（M4）

- 18 个动作都可从开发动作选择器到达。
- 同组转场的脚底/接触面抖动不超过 `2 logical px`。
- 非设计性关键锚点跳动不超过 `4 logical px`。
- 透明区点击下穿、人物区域点击与拖拽均正确。
- Win10/Win11、单双屏、混合 DPI、任务栏各方向、休眠恢复和拔屏通过手测。
- 待机 CPU 均值目标 `< 1%`，内存目标 `< 100 MB`，连续运行 8 小时无持续增长。
- 产出便携版和 Windows 安装包。

当前发布物：`dist/DesktopPet-0.3.0-win-x64.zip` 与 `dist/DesktopPet-Setup-0.3.0-win-x64.exe`。安装器固定写入当前用户的 `%LOCALAPPDATA%\Programs\DesktopPet`，更新使用暂存/备份回滚，卸载保留个人设置。

## 7. 预计工期

- M0 素材：1–3 个工作日（取决于是否能取得原始透明分图）
- M1 桌宠壳与点击穿透验证：1–2 个工作日
- M2 状态机与 18 姿势接入：1–2 个工作日
- M3 设置、多屏与 DPI：1–2 个工作日
- M4 测试与打包：1 个工作日

MVP 合计约 `6–10` 个工作日；高品质逐帧过渡另加 `3–7` 个工作日。

## 8. 开发与发布前置条件

- 当前机器已安装 .NET SDK 8.0.424；目标电脑运行 framework-dependent 便携包需要 .NET 8 Desktop Runtime x64。
- 必须先确认角色及生成素材的发布/商用授权。
- 18 张透明 RGBA 分图已重新制作；现有压平 JPG 仍只保留为原型参照。
