# DesktopPet 0.1.0 / 程序集 1.0.0 基线验收报告

## 结论

阶段 A 的第一轮基线检查已完成。后续重制应继承 0.1.0 的窗口、完整人物 PNG、透明显示和交互外壳，但不能照搬它的完整人物缩放与快速换姿势时间线。

本轮只读检查没有修改 `dist/DesktopPet-0.1.0-win-x64`、ZIP 或安装器，也没有创建新版工程。

## 1. 基线身份与完整性

- 基线目录：`dist/DesktopPet-0.1.0-win-x64`
- `DesktopPet.dll` 文件版本：`1.0.0.0`
- `DesktopPet.Core.dll` 文件版本：`1.0.0.0`
- 发布目录文件数：`25`
- ZIP SHA-256：`91E07A282CC8AA5C4E53AC751D17EA3BA0793D814519F95F5DD285B691917D3A`
- 安装器 SHA-256：`97BCE95D29FC8A1BF4F65329F0778C08FE20713B216D7BC47A5113BBF7103FC2`
- 检查结束时运行中的 `DesktopPet.exe`：`0`

目录组成：

- 程序与运行时文件：6 个
- `assets/manifest/assets.json`：1 个
- 完整人物 PNG：18 个
- `animations.json`：不存在
- 动画中间帧：不存在
- 内嵌图片：不存在；程序集仅内嵌 `app.baml` 与 `views/mainwindow.baml`

这说明 0.1.0 的正式运行素材就是 18 张完整人物图，不含后来版本的 160 帧动画素材。

## 2. 素材基线

发布清单参数：

- 源画布：`512×512`
- Alpha 点击阈值：`16`
- 比例母版：`idle.neutral`，即 06
- 目标站姿可见高度：`340px`
- 安全边距：`48px`
- 18 张人物 PNG 均存在，素材 lint 没有报告精灵文件损坏。

与眨眼问题直接相关的数据：

- `idle.neutral` 可见边界：`[172,124,168,340]`
- `idle.blink_smile` 可见边界：`[168,124,175,340]`
- 两张图虽然可见高度相同，但横向边界和完整人物轮廓不同。

因此 05 与 06 不是“只改变眼皮”的同构帧。新版本不得裁眼带、叠眼睛，也不得把两张图快速切换成眨眼动画。

## 3. 已发布 1.0 动作时间线

以下参数由反射直接读取 `dist/DesktopPet-0.1.0-win-x64/DesktopPet.Core.dll`，不是从 0.3.0 源码推测。

| 动作 | 0.1.0 的实际时间线 |
|---|---|
| 启动 | `01` 900ms → `06` 900ms |
| 待机 | `06` 1600ms、`07` 1200ms、`05` 220ms、`06` 1400ms，循环 |
| 坐姿 | `03` 2200ms → `04` 2200ms，循环 |
| 阅读 | `08` 900ms → `09` 2800ms → `12` 650ms → `09` 1800ms，循环 |
| 睡眠 | `10` 2400ms → `13` 2400ms，循环 |
| 爱心 | `11` 650ms → `14` 850ms → `06` 500ms |
| 招手 | `17` 500ms → `15` 420ms → `16` 420ms → `15` 420ms → `06` 500ms |
| 写字 | `18` 2100ms → `18` 2100ms，循环 |
| 拖拽 | `02` 每 500ms 循环 |
| 下落 | `02` 700ms → `06` 450ms |

0.1.0 还对完整人物图使用了以下变换：

- 待机、睡眠：`0.995–1.005` 全身缩放。
- 阅读：`0.997–1.003` 全身缩放。
- 爱心与拖拽：最高约 `1.005` 全身缩放。
- 坐姿与写字：`1px` 纵向移动。
- 招手：最高约 `±1°` 整体旋转。
- 下落：`-3px` 到 `+4px` 纵向移动，并有 `-3°` 到 `+3°` 整体旋转。

这些参数解释了 0.1.0 为什么比逐帧版安静，但它仍然包含用户已经明确要求取消的完整人物呼吸缩放和快速换姿势。重制版只继承其稳定外壳，不继承这些变换。

## 4. 已发布 1.0 自动调度

同样直接读取自 0.1.0 的已发布 DLL：

| 状态 | 最短停留 | 冷却 | 权重 | 后续状态 |
|---|---:|---:|---:|---|
| 启动 | 1800ms | 0 | 0 | 待机 |
| 待机 | 4000ms | 0 | 8 | 坐姿、阅读、睡眠、写字 |
| 坐姿 | 4400ms | 20s | 3 | 待机、阅读 |
| 阅读 | 6150ms | 35s | 2 | 待机 |
| 睡眠 | 9600ms | 60s | 1 | 待机 |
| 写字 | 8400ms | 45s | 2 | 待机 |
| 爱心 | 2000ms | 8s | 0 | 待机 |
| 招手 | 2260ms | 8s | 0 | 待机 |
| 拖拽 | 0 | 0 | 0 | 下落、待机 |
| 下落 | 1150ms | 0 | 1 | 待机 |

新版第一阶段应完全关闭该自动调度。只有全部手动动作验收后，才重新设计低频调度。

## 5. 当前运行环境与窗口基线

当前用户设置：

- 大小：`1.0`
- 不透明度：`1.0`
- 始终置顶：开启
- 锁定位置：关闭
- 点击区域：仅人物像素
- 动作频率：`1.0`
- 减少动态效果：关闭
- 开机启动：关闭
- 保存位置：`left=1196`、`top=556.8`、`DISPLAY1`

原生窗口检查结果：

- 设计逻辑尺寸：`300×360`
- 当前 125% DPI 下实测客户区：`375×450` 物理像素
- 无标题栏、不可缩放
- `WS_EX_LAYERED`：存在
- `WS_EX_TOPMOST`：存在
- `WS_EX_TOOLWINDOW`：存在
- 默认未设置整个窗口的 `WS_EX_TRANSPARENT`

这组窗口参数可以作为静态等价副本的外壳验收标准。

## 6. 发现的 0.1.0 发布问题

`assets.json` 为每张人物图声明了 `assets/masks/*.png`，但 0.1.0 发布包没有 `assets/masks` 目录，共缺少 18 张声明的命中掩码。

影响：

- 旧版自身仍可显示人物，因为运行时能从完整精灵 Alpha 建立点击数据。
- 现有素材 lint 会报告 18 个缺失掩码错误。
- 现有原生集成测试不能直接使用 0.1.0 发布清单读取掩码。
- 使用与 0.1.0 完全相同的开发版 `idle.neutral.png` 和对应只读掩码继续测试后，窗口样式与尺寸通过，但 `WM_NCHITTEST` 请求超时，因此本轮不把逐像素命中标记为自动验证通过。

新版不得修改旧包来补掩码。静态副本应使用完整人物 PNG 自身 Alpha 作为唯一命中来源，并另写不依赖外部 mask 文件的测试。

## 7. 启动闪图风险基线

0.1.0 发布包中只有 18 张正式人物 PNG，程序集中没有内嵌占位图片或测试图片。新版本必须进一步明确：

1. 窗口创建时图片控件保持隐藏。
2. 只允许从固定白名单加载 06。
3. 06 解码和 Alpha 校验成功后才显示图片控件。
4. 加载失败时保持透明，禁止回退到设计器图片、缓存图片或任意第一张资源。

## 8. 阶段 A 决策

可以进入阶段 B 的前提已经明确：

- 以 0.1.0 的窗口和 18 张完整 PNG 为基准。
- 初始版本只显示 `06_idle`。
- 不接入 0.1.0 的自动调度。
- 不接入 0.1.0 的全身缩放、整体旋转和待机换图。
- 不补绘、不生成、不拆分人物素材。
- 不把 05 和 06 组成眨眼。
- 新副本的点击测试直接使用当前完整 PNG 的 Alpha，不依赖缺失的外部 mask 文件。

根据整体计划，阶段 B 需在用户确认本报告后才开始，新工程暂定名为 `DesktopPet-1.0-Refined`。
