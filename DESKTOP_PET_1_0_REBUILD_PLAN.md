# DesktopPet 1.0 基线重制计划

## 0. 基线定义

本文中的“1.0版本”统一指当前最稳定、观感最正常的发布物：

- `dist/DesktopPet-0.1.0-win-x64/`
- `dist/DesktopPet-0.1.0-win-x64.zip`
- `dist/DesktopPet-Setup-0.1.0-win-x64.exe`

它们只作为运行基准和只读母版，禁止修改、覆盖、重新打包或用新版文件替换。

当前保护记录：

- 发布目录文件数：`25`
- ZIP SHA-256：`91E07A282CC8AA5C4E53AC751D17EA3BA0793D814519F95F5DD285B691917D3A`
- 安装器 SHA-256：`97BCE95D29FC8A1BF4F65329F0778C08FE20713B216D7BC47A5113BBF7103FC2`

`DesktopPet-NaturalMotion` 已废弃并删除，不再复用其中的代码、分层图、闭眼图、动作清单或设计结论。

## 1. 重制目标

这次不是重新制作一套“更复杂的动画”，而是先完整复现 0.1.0 的安静、稳定和人物一致性，再非常克制地增加动作。

核心目标：

1. 人物在任何时刻都必须使用原始完整人物图，不能重新绘制、生成、拼接或局部覆盖五官。
2. 默认画面接近静态，不做全身呼吸缩放、上下浮动、弹跳或持续摇摆。
3. 不把不同原图当作连续动画帧，不在相差较大的姿势之间快速切换。
4. 所有动作先以手动预览方式逐个完成，用户确认后才允许进入正式调度。
5. 第一阶段不启用自动大动作，避免桌宠在无人操作时频繁变化。

## 2. 不可违反的规则

- 不修改 0.1.0 的目录、ZIP 和安装器。
- 不使用 0.2.0、0.3.0 或旧 NaturalMotion 的动画素材作为新基线。
- 不调用图像生成来补眼睛、闭眼、手臂或中间帧。
- 不裁切眼带，不叠加眼皮，不对脸部做局部贴图。
- 不使用像素形变、AI 补帧、完整人物交叉淡化或动作间转场帧。
- 人物根节点缩放始终为 `1.0`，不得用缩放制造呼吸或落地效果。
- 同一动作期间人物完整图的尺寸、脚底锚点和窗口锚点保持不变。
- 原始 18 张图分别视为 18 个独立姿势，不默认视为一组连续帧。
- 未经单动作验收，不批量制作后续动作。

## 3. 总体实施阶段

### 阶段 A：记录 0.1.0 的真实基线

1. 运行原版，记录默认人物尺寸、窗口尺寸、初始位置、托盘菜单、拖拽、点击命中、退出方式和设置行为。
2. 录制至少 10 分钟原版画面，统计自动动作种类、间隔、停留时间和切换方式。
3. 在 100%、125%、150% 和 200% DPI 下截图，记录人物可见边界与脚底位置。
4. 记录启动过程，确认首张合法人物图出现前窗口应保持透明，禁止闪现未知图片。
5. 形成《0.1.0 基线验收表》。未完成这一步，不开始写新版功能。

### 阶段 B：建立完全静态的等价副本

整体方案确认后，才新建独立目录，暂定名为 `DesktopPet-1.0-Refined`。它不得覆盖任何既有发布目录。

1. 复现 0.1.0 的透明窗口、置顶、逐像素命中、拖拽、托盘菜单和退出功能。
2. 只加载原版 `06_idle` 完整图片。
3. 禁用自动动作、呼吸、缩放、旋转、淡入淡出和图片轮播。
4. 窗口必须保持透明，直到 `06_idle` 解码成功；失败时不显示占位图或测试图。
5. 新旧程序并排运行，对比人物大小、清晰度、位置和交互。

阶段 B 验收通过后，冻结窗口和素材加载底座。

### 阶段 C：建立“完整姿势”动作系统

动作系统只负责三类变化：

- 选择一张已经存在的完整人物图。
- 控制这张图保持多长时间。
- 必要时移动整个窗口坐标，例如探头、拖拽和下落。

动作系统不负责拆层、补帧、形变或人物缩放。

每个动作包含：`动作 ID / 原图 ID / 触发方式 / 保持时间 / 是否可中断 / 返回姿势`。默认返回 `06_idle`。

正式调度接入前必须提供一个调试面板，可手动选择单个姿势、暂停、返回待机，并显示当前图片 ID。调试面板不得自动轮播。

### 阶段 D：每个动作单独制作和验收

严格按照第 4 节顺序，一次只做一个动作。每个动作流程固定为：

1. 提交该动作的小计划。
2. 确认只使用哪一张原图；如有备选图，由用户选择。
3. 接入手动预览，不加入自动调度。
4. 连续测试 50 次触发、打断和返回待机。
5. 提交真实运行预览。
6. 用户确认后冻结该动作，再开始下一个。

### 阶段 E：最后再评估自动调度

所有手动动作验收前，自动调度保持关闭。

若后续启用：

- 待机可见时间不少于 `90%`。
- 两个自动动作之间至少间隔 `5 分钟`。
- 不连续选择两个整体轮廓差异很大的姿势。
- 自动动作一次只展示一张完整图，不在动作内部换图。
- 招手、欢呼、爱心和拖拽默认仍只由用户触发。
- 设置中提供“完全静态”开关，并默认开启。

## 4. 每个动作的初步计划

这些原图是独立姿势，不是动画帧。以下只定义用途，具体时长和触发方式在制作该动作前再单独确认。

| 顺序 | 动作 | 使用原图 | 实现原则 |
|---:|---|---|---|
| 1 | 基础待机 | `06_idle` | 永久静态母版；无呼吸、无缩放、无上下浮动。 |
| 2 | 微笑闭眼 | `05_idle_closed_smile` | 作为一次独立表情，不当作眨眼帧；短暂停留后直接回 `06`。 |
| 3 | 待机变体 | `07_idle_alt` | 作为低频独立姿势，不与 `06` 快速循环。 |
| 4 | 单手招手 | `15_wave_both` 或 `17_wave` | 先由用户选定一张；整张静态展示一次，不摆动手臂。 |
| 5 | 双手欢呼 | `16_wave_both_alt` | 独立点击反馈，不与单手招手组成动画。 |
| 6 | 爱心 | `11_heart` 或 `14_heart_tall` | 两张分开制作；不从低位爱心连续切到高位爱心。 |
| 7 | 站立阅读 | `08_read_small`、`09_read_hold`、`12_read_surprise` | 三张分别作为普通阅读、捧书、惊喜模式；不快速翻帧。 |
| 8 | 坐姿阅读 | `03_sit_book` 或 `04_sit_book_alt` | 先选一张作为正式坐姿；另一张仅保留为可选变体。 |
| 9 | 睡眠 | `10_sleep_sitting` 或 `13_sleep_sitting_alt` | 一次只选一张睡姿，长时间静态保持；不来回切换。 |
| 10 | 趴写 | `18_write_prone` | 整张静态保持，不移动笔尖，不制作书写循环。 |
| 11 | 屏幕探头 | `01_peek` | 图片不缩放；只移动窗口坐标从边缘进入和退出。 |
| 12 | 拖拽与下落 | `02_falling_or_reach` | 拖拽时使用完整原图；释放后的反馈只移动窗口坐标，不压缩或放大人物。 |

## 5. 关于眨眼的重新定义

当前阶段不制作局部眼睛动画。

原因是 `05` 与 `06` 的头部角度、眼镜、脸部轮廓和整体构图并非逐像素一致。把 `05` 裁成眼带覆盖 `06`，或者把两张图快速切换，都会产生已经出现过的眼部错位和人物跳动。

因此：

- `05` 只作为“闭眼微笑的完整独立姿势”。
- `06` 只作为“睁眼基础待机的完整独立姿势”。
- 两者不组成眨眼循环。
- 只有未来获得与 `06` 完全同画布、同头部、同眼镜、同身体，仅眼皮不同的原始完整帧时，才重新评估眨眼。

## 6. 姿势切换规则

- 不做动作之间的转场动画。
- 不叠加两张人物图，不交叉淡化。
- 用户触发后直接显示目标完整姿势，保持足够时间，再一次性返回 `06_idle`。
- 禁止在 1 秒内多次往返切图。
- 坐姿、睡姿、趴姿等轮廓变化大的动作，默认只允许从菜单手动触发。
- 切图前必须完成目标 PNG 解码；解码失败则继续显示当前合法图片，绝不显示占位图。

## 7. 人物大小统一方法

不在运行时动态调节头身比。统一工作只允许在动作接入前建立静态显示参数：

1. 对每张原图测量透明边界、头部宽度、人物可见高度和接触锚点。
2. 站姿以 `06_idle` 的可见高度和脚底为唯一基准。
3. 每张站姿只允许使用一个固定显示尺寸和固定偏移量，运行期间不变化。
4. 坐姿、睡姿和趴姿不强行拉到站姿高度，以头部尺度一致和接触面稳定为准。
5. 参数调整后生成对比接触表，由用户确认；不修改原 PNG 文件。

## 8. 测试与验收

### 旧版保护

- 实施前后重新计算 0.1.0 目录文件数、ZIP 和安装器 SHA-256。
- 任何数值变化立即停止发布。

### 启动稳定性

- 连续冷启动 50 次，不出现陌生图片、测试图、占位图、闪白或旧缓存帧。
- 首张合法帧加载前窗口为全透明。
- 素材路径错误时保持当前合法帧或透明退出。

### 视觉稳定性

- 根节点缩放始终为 `1.0`。
- 静态待机连续 30 分钟人物像素位置不变化。
- 站姿脚底逻辑坐标误差不超过 `1px`。
- 不出现局部五官错位、双影、轮廓拉伸或突然放大。

### 交互和系统

- 每个动作连续触发 50 次，返回待机一致。
- 透明区域穿透、人物区域拖拽和托盘退出正常。
- 覆盖 100%、125%、150%、200% DPI，多显示器和负坐标屏幕。
- 静止 CPU `<1%`，私有内存 `<100MB`，运行 30 分钟无持续增长。

## 9. 交付与审批门槛

本轮只交付本计划，不创建新工程，不生成新图片，不修改 0.1.0。

后续执行顺序：

1. 用户确认本整体计划。
2. 只完成阶段 A，提交《0.1.0 基线验收表》。
3. 用户确认基线报告。
4. 只完成阶段 B，提交完全静态的等价副本。
5. 用户确认静态副本。
6. 从“基础待机”开始，每个动作单独计划、单独实现、单独验收。

任何一步未通过，停留在当前步骤返修，不提前制作下一动作。
